export enum BILLING_FORM_ADDRESS_NAME {
  SHIPPING = 'shipping',
  BILLING = 'billing'
}

export enum MIDDLEWARE {
  IS_AUTHENTICATED = 'is-authenticated',
  IS_ANONYMOUS_CART = 'is-anonymous-cart',
  HAS_ERRORS = 'has-errors'
}

export const CART_USER = 'cart-user';
export const CART_ID_COOKIE_NAME = 'CART_ID';
export const ACCEPTED_COOKIES_COOKIE_NAME = 'cookies:accepted';

export enum IMAGE_TAGS {
  IMAGE = 'img',
  NUXT_IMAGE = 'nuxt-img'
}
